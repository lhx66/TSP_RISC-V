# LSU 非对齐访问实现说明

## 已完成的修改

### 1. exu_ls.v - 支持非对齐Store
- ✅ 添加了状态机（IDLE, BUSY1, BUSY2）
- ✅ 添加了对齐检测逻辑
- ✅ 实现了非对齐SH/SW的多周期写入
  - SH: 最多2个周期（地址%4=3时）
  - SW: 最多2个周期（地址%4!=0时）
- ✅ 正确生成字节使能和数据移位

### 2. ls_ctrl.v - 添加Load数据重组框架
- ⚠️ 添加了Load数据重组逻辑，但不完整
- ❌ 标记了TODO：需要支持多周期Load数据累积

## 当前限制

### Load指令的非对齐支持不完整
**问题**：非对齐Load需要从两个地址读取数据并组装，但当前架构不支持：

**示例**：`lw a0, 3(sp)`
```
第1周期：读取 sp+0 (对齐地址) -> 获取字节0,1,2,3
          需要字节3 -> data[31:24]

第2周期：读取 sp+4 (对齐地址) -> 获取字节4,5,6,7
          需要字节4,5,6 -> data[23:0]

最终：a0 = {字节4,5,6, 字节3}
```

当前ls_ctrl无法累积这两个周期的数据。

## 解决方案

### 方案A：在exu_ls中累积Load数据（推荐）

修改步骤：
1. 在exu_ls中添加 `load_data_acc_r` 寄存器
2. 从ls_ctrl获取每个周期的Load数据
3. 根据状态机和字节偏移量组装最终数据
4. 在完成时发送到wb_arbiter

需要修改的接口：
```verilog
// ls_ctrl -> exu_ls
input wire [31:0] ls_rdata_i,      // 从AXI读取的数据
input wire        ls_rvalid_i,     // 数据有效
```

### 方案B：在ls_ctrl中累积Load数据

修改步骤：
1. ls_ctrl跟踪Load周期数
2. 添加非对齐Load状态机
3. 累积多次读取的数据
4. 重组后写回

需要修改：
- 添加状态：LOAD_ACCUM（累积第2次数据）
- 添加寄存器：load_data_part1（第1次读取的数据）
- 修改逻辑：AXI_R状态需要判断是否需要等待第2次读取

## 快速测试建议

### 测试当前Store修复
1. 综合当前的RTL代码
2. 运行CoreMark测试
3. 观察是否有改善

### 如果仍有Load相关错误
可以临时方案：
- 禁用CoreMark中涉及非对齐Load的优化
- 或使用-O0编译（减少但无法完全消除非对齐访问）

## 长期解决方案

完整的非对齐Load支持需要：
1. 实现方案A或方案B
2. 仿真验证所有对齐情况
3. 综合测试

预计工作量：2-4小时
